spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: postgresql://localhost:5432/jiro
    username: ucemuser
    password: ucempw
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      show-sql: true
      ddl-auto: update